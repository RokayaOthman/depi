QuerySelectorAll => returns a nodelist not an array 

The name attribute in an HTML <input> => element is essential for identifying and processing form data when it is submitted to a server.
    - it defines the key used to send the input's value 


localStorage can only store strings , so in case of an array or a object you have to first convert them to string using stringify


indexOf : returns the position of the first matching object reference in the array